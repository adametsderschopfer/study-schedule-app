import { StyledLoaderIndicator } from "./index";

export default {
  title: "ui/Loader indicators",
  component: StyledLoaderIndicator,
};

export const DefaultLoaderIndicator = (): JSX.Element => (
  <StyledLoaderIndicator />
);
